{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "go build -o identity-service ./cmd/server"
  },
  "deploy": {
    "startCommand": "./identity-service",
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE"
  }
}