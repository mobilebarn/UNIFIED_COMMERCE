{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "go build -o order-service ./cmd/server"
  },
  "deploy": {
    "startCommand": "./order-service",
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE"
  }
}