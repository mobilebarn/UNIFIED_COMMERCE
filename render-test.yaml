services:
  - type: web
    name: test-web-service
    runtime: go
    plan: free
    buildCommand: go build -o app ./cmd/server
    startCommand: ./app
    rootDir: services/identity
    
  - type: pserv
    name: test-postgres
    plan: free